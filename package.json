{
  "name": "era-dal-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "local": "npm run build && wrangler d1 execute era-dal --local --file=migrations/001_core.sql 2>/dev/null; wrangler pages dev --port 8788",
    "setup": "npm install && npm run build && wrangler d1 execute era-dal --local --file=migrations/001_core.sql",
    "package": "bash package-release.sh",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.1"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}